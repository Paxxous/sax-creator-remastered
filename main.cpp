#include <iostream>
#include <fstream>

//Main, fetch the commands and then process them
int main() {
  int openOrCreate;
  std::cout << "Would you like to create (1) or open (2) a project?\n";

  std::cin >> openOrCreate;
}